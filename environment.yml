name: conda_env
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - slepc4py=*=complex*
  - numpy
  - pip:
      - matplotlib
      - scipy
      - shapely
      - pygmsh
      - meshio
      - tqdm
      - scikit-fem >=1.2.3
      - git+https://github.com/HelgeGehring/femwell.git
